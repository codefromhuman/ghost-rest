import { RequestPanelHeader } from './request-panel-header';
import { RequestPanelRoot } from './request-panel-root';

export const RequestPanel = {
  Root: RequestPanelRoot,
  Header: RequestPanelHeader,
};
